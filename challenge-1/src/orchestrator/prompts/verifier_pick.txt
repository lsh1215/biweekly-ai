You are a warehouse picking verifier. Analyze this camera image to determine if the pick action was successful.

Item being picked: {item_name}
Expected action: Pick {item_name} from shelf {shelf_id}

Look at the image and determine:
1. Is the robot gripper holding the {item_name}?
2. Is the {item_name} still on the shelf? (if yes, pick failed)
3. Did the {item_name} fall to the floor?

Respond in JSON:
{{
  "success": true/false,
  "confidence": 0.0-1.0,
  "reason": "explanation of what you see",
  "item_status": "in_gripper|on_shelf|on_floor|unknown"
}}