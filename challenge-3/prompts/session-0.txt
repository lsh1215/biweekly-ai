너는 challenge-3의 Sprint 0 자율 실행자다. 사용자는 자고 있다. 무인 실행. 질문 금지.

## 컨텍스트

cwd: /Users/leesanghun/My_Project/agent-engineering/biweekly-ai/challenge-3

먼저 다음 파일을 읽어 (Read 도구):
- PRD.md (모든 락 결정)
- EXECUTION_PLAN.md (Sprint 0 산출물 + checkpoint)
- HARNESS.md
- ../CLAUDE.md (12 원칙, 특히 §9 환경 propagation, §12 API 키 hard-check 금지)
- ../challenge-2/RETRO.md (cascade fail 4 사례)
- ~/.claude/agents/blog-writer.md, ~/.claude/agents/ai-tell-scrubber.md (포팅 원본)
- ~/.claude/skills/blog/SKILL.md, philosophy.md, style-rules.md, templates.md, argumentation.md, ai-tell-rules.md (포팅 원본)
- ~/.claude/plugins/marketplaces/claude-plugins-official/plugins/example-plugin/.claude-plugin/plugin.json (spec 레퍼런스)

## 무인 실행 원칙 (CLAUDE.md §1)

- 모호한 결정은 자체 결정 후 TIMELINE.md에 "왜 그 선택을 했는지" 기록
- 사용자에게 질문·확인 요청 금지
- 실패 2회 재시도 후 `.half_scope=<reason>` 발동 + 즉시 exit

## Sprint 0 미션

EXECUTION_PLAN.md의 "Sprint 0" 섹션 그대로 실행. 산출물 30+ 파일.

핵심:
1. **TDD 강제**: 테스트 6개 (`tests/test_*.py`) 먼저 작성, 모두 fail 확인
2. `.venv` FORCED: `python3 -m venv .venv` + `source .venv/bin/activate` + `pip install -r requirements.txt`
3. 포팅: `~/.claude/skills/blog/` 6 file → `aiwriting/skills/blog/`. **반드시** path rewrite (`/Users/leesanghun/.claude/skills/blog/foo.md` → `skills/blog/foo.md (plugin-relative)`) + R6→R7 in-place sed (`R1–R6` → `R1–R7`)
4. `~/.claude/agents/blog-writer.md` → `aiwriting/agents/aiwriting-writer.md` (Sprint 0에선 stub 수준, Sprint 1에서 universal로 확장)
5. `~/.claude/agents/ai-tell-scrubber.md` → `aiwriting/agents/aiwriting-scrubber.md`
6. **5 파일 stub 생성** (Critic C2 fix): `scripts/checkpoint_sprint{0,1,2,3}.sh` + `VERIFY.sh`. 모두 첫 4 라인 동일:
   ```bash
   #!/bin/bash
   set -euo pipefail
   cd "$(dirname "$0")/.." # VERIFY.sh는 cd "$(dirname "$0")"
   [ -d ".venv" ] || { echo "ERR: .venv missing (D5)"; exit 3; }
   source .venv/bin/activate
   ```
   Sprint 0 checkpoint만 full content. 나머지 4개는 위 4 라인 + `echo "sprint{N} STUB - to be filled in sprint{N}"; exit 0` 정도의 skeleton.
7. plugin.json metadata 4 필드만 (PRD §4 그대로)
8. marketplace.json `source: "./"`
9. README.md (한계 명시 — copy-killer 외부 detector 통과 보장 X), LICENSE (MIT, Copyright (c) 2026 Sanghun Lee)
10. fixture inputs 16개 (PRD §3 D10 토픽 lock, 4 format × 4 topic)
11. `known_facts.yml.example` (사용자 yaml 템플릿)
12. `scripts/cost_probe.py` (정적 추정만, `claude -p "ping"` 호출 금지)
13. `scripts/validate_manifest.py` (jsonschema fallback, S1 시나리오)
14. blog/SKILL.md Phase 5 graceful skip guard (D4): 첫 줄에
    ```
    # Phase 5 (Notion publish) — graceful skip if MCP unavailable
    if Notion MCP not available:
      notice "Notion MCP not configured, draft saved locally at {path}"
      return success
    ```

## 절대 위반 금지

- `~/.claude/*` 절대경로 plugin 안에 잔존 0건 (`grep -r "/Users/leesanghun" aiwriting/` → 0)
- `R1–R6`, `R1-R6` 잔존 0건 (R6→R7 migration)
- `docker-compose` (하이픈) 사용 0건
- `claude -p "ping"` 또는 ANTHROPIC_API_KEY env var 검사 0건 (CLAUDE.md §12)
- 테스트 없이 다음 단계 진행 0건 (CLAUDE.md §2 TDD)

## 종료 조건

`bash scripts/checkpoint_sprint0.sh` exit 0 통과. 통과 시:
1. `git add -A && git commit -m "feat(challenge-3): sprint 0 — plugin scaffold + blog port + 5 checkpoint stubs"` (cwd가 `/Users/leesanghun/My_Project/agent-engineering/biweekly-ai`라서 `git -C ..` 사용 가능)
2. TIMELINE.md에 `$(date -Iseconds) sprint-0 DONE commit=<hash>` append
3. 정상 종료

## 실패 시

2회 재시도 후 fail 시:
1. `.half_scope=sprint0_<reason>` 파일 생성 (예: `sprint0_portability_fail`)
2. TIMELINE.md에 실패 사유 + 시도 횟수 append
3. exit 1

## 권한

`--dangerously-skip-permissions` 모드. 자유롭게 Bash, Write, Edit 사용.

지금 시작.
