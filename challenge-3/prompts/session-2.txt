너는 challenge-3의 Sprint 2 자율 실행자다. 사용자는 자고 있다. 무인 실행.

## 컨텍스트

cwd: /Users/leesanghun/My_Project/agent-engineering/biweekly-ai/challenge-3

cascade skip 체크:
```bash
[ -f .half_scope ] && { echo "HALF_SCOPE: $(cat .half_scope) - sprint 2 skip"; exit 0; }
```

읽어:
- PRD.md, EXECUTION_PLAN.md (Sprint 2 섹션)
- ../CLAUDE.md
- aiwriting/agents/aiwriting-writer.md, aiwriting-scrubber.md (Sprint 0/1 산출)
- replay/fixtures/ (Sprint 1 녹화 결과)

## Sprint 2 미션

핵심 결정 (Critic C1 fix): **copy-killer + fact-checker는 LLM 호출 0회**. Pure Python regex + 가중치 score.

1. **TDD 우선**: 테스트 작성
   - `tests/test_copy_killer_weights.py` (합 1.0, threshold 0.35)
   - `tests/test_copy_killer_metric_*.py` (6개 — 각 지표 단위 테스트)
   - `tests/test_copy_killer_threshold_tuning.py` (S3 자동 튜닝 결정성)
   - `tests/test_structure_critic_modes.py` (4 mode section 단일 .md 파싱)
   - `tests/test_e2e_replay_critic.py` (16 critic replay → APPROVE/ITERATE/REJECT 결정성)

2. **scrubber 일반화** (`aiwriting/agents/aiwriting-scrubber.md`):
   - Sprint 0 포팅된 거 (blog 전용)을 4 format 적용 가능하게 확장
   - format param 인식 (입력 prompt에 `format: blog | cover-letter | paper | letter`)
   - R1~R7 grep gate 모든 format 공용
   - format별 추가 규칙: cover-letter는 "최선을 다해 / 꾸준히" 더 엄격

3. **copy-killer agent + script** (LLM-free):
   - `aiwriting/agents/aiwriting-copy-killer.md` — agent 본문은 "scripts/copy_killer.py 호출하라"는 안내만 (LLM은 호출 X)
   - `scripts/copy_killer.py <md_path> --threshold 0.35`
   - `scripts/copy_killer_metrics.py` — 6 지표 함수 (PRD §3 D6)
   - 가중치: sentence_length_variance 0.20, avg_syllable_length 0.10, connector_frequency 0.20, r1_r7_residual 0.30, monotone_ending_ratio 0.15, generic_modifier_density 0.05
   - threshold default 0.35
   - **자동 튜닝 룰** (S3): 16 fixture fail > 50%면 ±0.05 1회 자동 조정. 그래도 fail > 50%면 weight 균등 1/6 reset. LLM 판단 없음, deterministic.

4. **structure-critic agent** (단일 .md + 4 mode section):
   - `aiwriting/agents/aiwriting-structure-critic.md`
   - frontmatter: `model: opus`, `tools: Read, Grep`, disallowedTools: Write, Edit
   - 본문에 `## Common`, `## Mode: blog`, `## Mode: cover-letter`, `## Mode: paper`, `## Mode: letter` 5 섹션
   - 입력 prompt에 `mode: blog` 명시 → 해당 mode section만 적용
   - verdict: APPROVE / ITERATE / REJECT 명시
   - test_structure_critic_modes.py가 4 mode section 존재 검증

5. **structure-critic replay 라이브 녹화** (Sprint 2 only):
   - 16 fixture × structure-critic = 16 라이브 호출 (~$1.05 Opus)
   - `replay/fixtures/{format}/{slug}-critic.json` 저장 (D7 shape)
   - S5/S6 동일 적용

6. **`scripts/run_sprint2_pipeline.py`**:
   - 16 fixture × scrubber + copy-killer + structure-critic-replay
   - `fixtures/outputs/sprint2/{format}-{slug}.md` 16개 산출
   - `fixtures/outputs/sprint2/{format}-{slug}.report.json` 16개 (copy-killer score + critic verdict)
   - 멱등화: 진입 시 `rm -rf fixtures/outputs/sprint2/`

## 종료 조건

`bash scripts/checkpoint_sprint2.sh` exit 0:
1. git commit `feat(challenge-3): sprint 2 — scrubber generalize + copy-killer (LLM-free) + structure-critic`
2. TIMELINE.md `sprint-2 DONE commit=<hash>` append
3. 정상 종료

## 실패 시

- 2회 재시도 → `.half_scope=sprint2_<reason>`
- copy-killer threshold 자동 튜닝 1회 후 여전히 fail → weight 균등 reset 시도 후 fail → `.half_scope=copy_killer_thresholds`
- TIMELINE 갱신

## 권한

`--dangerously-skip-permissions` 모드.

지금 시작.
